application_name: SAMPLE_APP
home_dir: /home/eddiehftang
app_binary_dir: /opt/sample-app/${p:host_label}
app_service_account: eddiehftang
app_service_account_group: eddiehftang
kerberos_keytab_path:
kerberos_tkt_path:
download_dir: ${p:app_binary_dir}/download
app_log_dir: /var/log/sample-app/${p:host_label}
deploy_dir: ${p:app_binary_dir}/deploy
download_deploy_dir: ${p:app_binary_dir}/download/deploy